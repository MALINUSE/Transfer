<%= form_for(@people_sim) do |f| %>
  <% if @people_sim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@people_sim.errors.count, "error") %> prohibited this people_sim from being saved:</h2>

      <ul>
      <% @people_sim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :people_id %><br>
    <%= f.text_field :people_id %>
  </div>
  <div class="field">
    <%= f.label :sim_carte_id %><br>
    <%= f.text_field :sim_carte_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
