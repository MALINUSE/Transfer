<!-- Fixed navbar -->
<div class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'TOUNGA', root_path, :class => "navbar-brand", id: "logo" %>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to ' ', root_path, class: "glyphicon glyphicon-home glyphiconhead", :title => "Home" %></li>
        <li><a href="#about">About</a></li>
        <li><a href="#contact">Contact</a></li>
        <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">Tiers</a>
              <ul class="dropdown-menu">
                <li><%= link_to 'CLIENTS', people_path %></li>
              </ul>
            </li>

        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle glyphicon glyphicon-user glyphiconhead" data-toggle="dropdown" , title="User"></a>
              <ul class="dropdown-menu">
                <li><%= link_to "#{current_user.role}", "#" %></li>
                <li><%= link_to 'Edit profile', edit_user_registration_path %></li>
                <li class="divider"></li>
                <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
              </ul>
            </li>
            <% if current_user.role == 'Root' %>
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle glyphicon glyphicon-wrench glyphiconhead" data-toggle="dropdown"></a>
                  <ul class="dropdown-menu">
                    <li><%= link_to 'OUTILS', "#" %></li>
                  </ul>
                </li>
            <% end %>

        <% else %>
            <!--<li><%= link_to "Sign up", new_user_registration_path %></li>-->
            <li><%= link_to "", new_user_session_path(login_options), class: "glyphicon glyphicon-log-in glyphiconhead", :title => "Log in" %></li>
        <% end %>

      </ul>
    </div>
    <!--/.nav-collapse -->
  </div>
</div>